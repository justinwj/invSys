# invSys Static Implementation Manifest

Synthetic fixture expectations:

- Two build packages: Core and Operations.
- Direct calls, a literal cross-XLAM bridge, and one unresolved dynamic call.
- Ribbon, `Auto_Open`, UserForm, processor, string-dispatch, test, Windows-callback, and compatibility roots.
- Managed `System_Key` and `Condition` headers with preserved `Custom_Color`.
- A `RETIRED_ROW_HEADER` warning for the negative legacy fixture.
